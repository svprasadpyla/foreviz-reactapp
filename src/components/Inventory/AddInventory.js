import React, { Component } from "react";

class AddInventory extends Component {
  constructor(props) {
    super(props);
    this.state = {
      
    };
  }
  componentDidMount() {
    
  }

  componentDidUpdate(prevProps, prevState){
  
  }

  render() {
    
    return (
      <div> AddInventory is in development</div>
      
    );
  }
}

export default AddInventory;
